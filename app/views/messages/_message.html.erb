<%# Needs to be fixed, issue lies here, most likely %>
<div class="message <%= message.user == current_user ? 'user-message' : 'other-message' %>" data-message-user-id="<%= message.user.id %>">
  <small>
    <strong><%= message.user.first_name %></strong>
    <i class="ms-2 grey"><%= message.created_at.strftime("%a, %d.%m. %H:%M") %></i>
  </small>
  <p class="mb-0"><%= message.content %></p>
</div>
